import { NextResponse } from 'next/server';

// Placeholder route for future escaleta-specific generation features.
export async function GET() {
  return NextResponse.json({ message: 'Ruta de generación de escaleta no implementada aún.' }, { status: 501 });
}
